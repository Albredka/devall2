package master;

public class Pawns extends Piece {

    public Pawns(boolean available, int x, int y) {
        super(available, x, y);
    }

    //Override of the method validMove
    //Checks if the build of the movement is correctly done.
    //Checks if the movement is valid for a Pawn
    
}
